import 'package:flutter/material.dart';

class HistoriView extends StatelessWidget {
  const HistoriView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
